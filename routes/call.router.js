const express=require("express"),controller=require("../controllers/callController"),{requireAuth}=require("../middleware/authMiddleware");const router=express.Router();router.use(requireAuth);router.get("/history",controller.history);router.get("/active",controller.active);router.get("/ice-servers",controller.iceServers);router.get("/:callId",controller.details);module.exports=router;
