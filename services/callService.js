const Call=require("../models/callSessionModel"),Participant=require("../models/callParticipantModel"),User=require("../models/userModel"),permission=require("./callPermissionService"),presence=require("./presenceService"),{createNotification}=require("./notificationService");
const id=permission.id,terminal=new Set(["rejected","cancelled","missed","ended","failed"]),transitions={initiating:new Set(["ringing","active","failed"]),ringing:new Set(["active","rejected","cancelled","missed","failed"]),active:new Set(["ended","failed"])};
function assertCallTransition(from,to){if(!transitions[from]?.has(to))throw permission.error("CALL_INVALID_STATE",`Cannot change a call from ${from} to ${to}`,409);return true;}
async function isBusy(userId,{ParticipantModel=Participant}={}){const rows=await ParticipantModel.find({user:userId,status:{$in:["ringing","joined"]}}).populate("call").limit(10);return rows.some(row=>row.call&&["ringing","active"].includes(row.call.status));}
async function safeCall(call,deps={}){const ParticipantModel=deps.ParticipantModel||Participant,participants=await ParticipantModel.find({call:call._id}).populate("user","_id username name avatarUrl").lean();const raw=call.toObject?call.toObject():{...call};return{...raw,participants};}
async function createDirectCall(callerId,recipientId,input={},deps={}){const access=await permission.assertCanStartDirectCall(callerId,recipientId,{conversationId:input.conversationId,...deps});if(await isBusy(callerId,deps))throw permission.error("CALL_ALREADY_ACTIVE","Leave your current call before starting another",409);if(await isBusy(recipientId,deps))throw permission.error("CALL_RECIPIENT_BUSY","The recipient is busy",409);if(!input.allowOffline&&!presence.isOnline(recipientId))throw permission.error("CALL_RECIPIENT_OFFLINE","The recipient is offline",409);const CallModel=deps.CallModel||Call,ParticipantModel=deps.ParticipantModel||Participant,call=await CallModel.create({callType:"direct",mediaMode:input.mediaMode==="video"?"video":"audio",conversation:access.conversation._id,initiatedBy:callerId,status:"ringing",maxParticipants:2});await ParticipantModel.create([{call:call._id,user:callerId,role:"host",status:"joined",joinedAt:new Date(),cameraEnabled:call.mediaMode==="video"},{call:call._id,user:recipientId,role:"participant",status:"ringing",invitedBy:callerId,cameraEnabled:false}]);return safeCall(call,deps);}
async function createContextualCall(userId,input={},deps={}){let context,fields={};if(input.callType==="repository"){context=await permission.assertCanStartRepositoryCall(input.repositoryId,userId,deps);fields.repository=context.repository._id;}else if(input.callType==="issue"){context=await permission.assertCanStartIssueCall(input.issueId,userId,deps);fields={repository:context.issue.repository,issue:context.issue._id};}else if(input.callType==="pull_request"){context=await permission.assertCanStartPullRequestCall(input.pullRequestId,userId,deps);fields={repository:context.pullRequest.repository,pullRequest:context.pullRequest._id};}else if(input.callType==="mentor"){context=await permission.assertCanStartMentorCall(input.mentorRequestId,userId,deps);fields={repository:context.request.repository,issue:context.request.issue||null,mentorRequest:context.request._id,conversation:context.request.conversation||null};}else if(input.callType==="guided_contribution"){context=await permission.assertCanStartContributionCall(input.contributionSessionId,userId,deps);fields={repository:context.session.repository,issue:context.session.issue,contributionSession:context.session._id,mentorRequest:context.session.mentorRequest||null};}else throw permission.error("CALL_INVALID_STATE","Invalid contextual call type",400);if(await isBusy(userId,deps))throw permission.error("CALL_ALREADY_ACTIVE","Leave your current call before starting another",409);const call=await (deps.CallModel||Call).create({callType:input.callType,mediaMode:input.mediaMode==="video"?"video":"audio",initiatedBy:userId,status:"active",answeredAt:new Date(),maxParticipants:4,...fields});await (deps.ParticipantModel||Participant).create({call:call._id,user:userId,role:context.role==="mentor"?"mentor":"host",status:"joined",joinedAt:new Date(),cameraEnabled:call.mediaMode==="video"});return safeCall(call,deps);}
async function getCallById(callId,userId,deps={}){const {call}=await permission.assertCanJoinCall(callId,userId,deps);return safeCall(call,deps);}
async function acceptCall(callId,userId,input={},deps={}){const {call,participant}=await permission.assertParticipant(callId,userId,deps);if(call.status!=="ringing"||participant.status!=="ringing")throw permission.error("CALL_INVALID_STATE","This call cannot be accepted",409);if(await isBusy(userId,{...deps,excludeCall:callId})){await finish(call,"rejected",userId,"busy",deps);throw permission.error("CALL_RECIPIENT_BUSY","Leave your current call before accepting",409);}assertCallTransition(call.status,"active");call.status="active";call.answeredAt=new Date();await call.save();participant.status="joined";participant.joinedAt=new Date();participant.microphoneEnabled=input.microphoneEnabled!==false;participant.cameraEnabled=call.mediaMode==="video"&&input.cameraEnabled!==false;await participant.save();return safeCall(call,deps);}
async function rejectCall(callId,userId,deps={}){const {call,participant}=await permission.assertParticipant(callId,userId,deps);if(call.status!=="ringing"||participant.status!=="ringing")throw permission.error("CALL_INVALID_STATE","This call cannot be rejected",409);participant.status="rejected";participant.leftAt=new Date();await participant.save();return finish(call,"rejected",userId,"rejected",deps);}
async function cancelCall(callId,userId,deps={}){const {call,participant}=await permission.assertParticipant(callId,userId,deps);if(id(call.initiatedBy)!==id(userId)||call.status!=="ringing")throw permission.error("CALL_INVALID_STATE","Only the caller can cancel a ringing call",409);participant.status="left";participant.leftAt=new Date();await participant.save();return finish(call,"cancelled",userId,"cancelled",deps);}
async function finish(call,status,userId,reason,deps={}){if(call.status!==status)assertCallTransition(call.status,status);call.status=status;call.endedAt=new Date();call.endedBy=userId||null;call.endReason=reason;call.screenSharingUser=null;await call.save();await (deps.ParticipantModel||Participant).updateMany({call:call._id,status:{$in:["joined","ringing","invited"]}},{$set:{status:status==="missed"?"missed":"left",leftAt:new Date(),screenSharing:false,handRaised:false,lastConnectionState:"closed"}});return safeCall(call,deps);}
async function endCall(callId,userId,reason="participant_ended",deps={}){const {call}=await permission.assertParticipant(callId,userId,deps);if(call.status!=="active")throw permission.error("CALL_NOT_ACTIVE","This call is not active",409);return finish(call,"ended",userId,id(call.initiatedBy)===id(userId)?"caller_ended":reason,deps);}
async function joinCall(callId,userId,input={},deps={}){const value=await permission.assertCanJoinCall(callId,userId,deps),ParticipantModel=deps.ParticipantModel||Participant;if(await isBusy(userId,deps))throw permission.error("CALL_RECIPIENT_BUSY","Leave your current call before joining",409);const joined=await ParticipantModel.countDocuments({call:value.call._id,status:"joined"});if(joined>=value.call.maxParticipants)throw permission.error("CALL_PARTICIPANT_LIMIT","This call has reached the current participant limit",409);let participant=value.participant||await ParticipantModel.findOne({call:value.call._id,user:userId});if(!participant)participant=await ParticipantModel.create({call:value.call._id,user:userId,role:value.role||"participant",status:"joined",joinedAt:new Date()});else{participant.status="joined";participant.joinedAt=participant.joinedAt||new Date();participant.leftAt=null;participant.lastConnectionState="connecting";participant.microphoneEnabled=input.microphoneEnabled!==false;participant.cameraEnabled=value.call.mediaMode==="video"&&input.cameraEnabled!==false;await participant.save();}return safeCall(value.call,deps);}
async function leaveCall(callId,userId,deps={}){const {call,participant}=await permission.assertParticipant(callId,userId,deps);participant.status="left";participant.leftAt=new Date();participant.screenSharing=false;participant.handRaised=false;participant.lastConnectionState="closed";await participant.save();if(id(call.screenSharingUser)===id(userId)){call.screenSharingUser=null;await call.save();}const remaining=await (deps.ParticipantModel||Participant).countDocuments({call:call._id,status:"joined"});if(!remaining&&call.status==="active")return finish(call,"ended",userId,"participant_ended",deps);return safeCall(call,deps);}
async function inviteParticipant(callId,inviterId,invitedUserId,deps={}){const {call}=await permission.assertCanInviteParticipant(callId,inviterId,invitedUserId,deps);const ParticipantModel=deps.ParticipantModel||Participant,count=await ParticipantModel.countDocuments({call:call._id,status:{$in:["invited","ringing","joined"]}});if(count>=call.maxParticipants)throw permission.error("CALL_PARTICIPANT_LIMIT","This call has reached the current participant limit",409);let participant;try{participant=await ParticipantModel.create({call:call._id,user:invitedUserId,invitedBy:inviterId,status:"invited"});}catch(error){if(error.code===11000)throw permission.error("CALL_ALREADY_ACTIVE","This user is already associated with the call",409);throw error;}await createNotification({recipient:invitedUserId,actor:inviterId,repository:call.repository,type:"call_invitation",title:"CodeHub call invitation",message:"You were invited to a CodeHub call.",url:`/calls/${call._id}`,eventKey:`call-invite:${call._id}:${invitedUserId}`});return participant;}
async function removeParticipant(callId,hostId,userId,deps={}){const host=await permission.assertParticipant(callId,hostId,deps);if(host.participant.role!=="host")throw permission.error("CALL_FORBIDDEN","Only the host may remove participants");if(id(userId)===id(hostId))throw permission.error("CALL_INVALID_STATE","Use Leave call instead",400);const participant=await (deps.ParticipantModel||Participant).findOne({call:callId,user:userId});if(!participant)throw permission.error("CALL_NOT_FOUND","Participant not found",404);participant.status="removed";participant.leftAt=new Date();participant.screenSharing=false;participant.handRaised=false;await participant.save();if(id(host.call.screenSharingUser)===id(userId)){host.call.screenSharingUser=null;await host.call.save();}return participant;}
async function updateParticipant(callId,userId,patch,deps={}){const {call,participant}=await permission.assertParticipant(callId,userId,deps);if(call.status!=="active"||participant.status!=="joined")throw permission.error("CALL_NOT_ACTIVE","This call is not active",409);for(const key of ["microphoneEnabled","cameraEnabled","handRaised","lastConnectionState"])if(patch[key]!==undefined)participant[key]=patch[key];if(patch.handRaised===true)participant.handRaisedAt=new Date();if(patch.handRaised===false)participant.handRaisedAt=null;await participant.save();return participant;}
async function setScreenShare(callId,userId,active,deps={}){const {call,participant}=await permission.assertCanShareScreen(callId,userId,deps);if(active){call.screenSharingUser=userId;participant.screenSharing=true;}else if(id(call.screenSharingUser)===id(userId)){call.screenSharingUser=null;participant.screenSharing=false;}await Promise.all([call.save(),participant.save()]);return{call,participant};}
async function markCallMissed(callId,deps={}){const call=await (deps.CallModel||Call).findById(callId);if(!call||call.status!=="ringing")return null;const recipients=await (deps.ParticipantModel||Participant).find({call:call._id,status:"ringing"});const result=await finish(call,"missed",null,"no_answer",deps);for(const row of recipients)await createNotification({recipient:row.user,actor:call.initiatedBy,type:"missed_call",title:"Missed CodeHub call",message:"You missed a CodeHub call.",url:"/calls/history",eventKey:`missed-call:${call._id}:${row.user}`});return result;}
async function getActiveCallForUser(userId,deps={}){const rows=await (deps.ParticipantModel||Participant).find({user:userId,status:{$in:["ringing","joined","invited"]}}).populate("call");const row=rows.find(value=>value.call&&["ringing","active"].includes(value.call.status));return row?safeCall(row.call,deps):null;}
async function getCallHistory(userId,deps={}){const ParticipantModel=deps.ParticipantModel||Participant,rows=await ParticipantModel.find({user:userId}).sort({createdAt:-1}).limit(100).populate({path:"call",populate:{path:"initiatedBy",select:"_id username name avatarUrl"}}).lean();return rows.filter(row=>row.call).map(row=>({...row.call,currentParticipant:{role:row.role,status:row.status}}));}
module.exports={terminal,transitions,assertCallTransition,isBusy,safeCall,createDirectCall,createContextualCall,getCallById,acceptCall,rejectCall,cancelCall,endCall,joinCall,leaveCall,inviteParticipant,removeParticipant,updateParticipant,setScreenShare,markCallMissed,getActiveCallForUser,getCallHistory,finish};
