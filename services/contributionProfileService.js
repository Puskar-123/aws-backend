const {EXPERIENCE_LEVELS,AVAILABLE_TIMES,normalizeSkill,normalizeTaskType,normalizeToken}=require("../constants/contributionConstants");
const error=(code,message,status=400)=>Object.assign(new Error(message),{code,status});
function normalizeList(values,normalizer,limit=30){if(!Array.isArray(values)||values.length>limit)throw error("INVALID_SKILL_PROFILE","Invalid profile values");const result=[];for(const value of values){const normalized=normalizer(value);if(!normalized)throw error("INVALID_SKILL_PROFILE",`Unsupported value: ${String(value).slice(0,80)}`);if(!result.includes(normalized))result.push(normalized);}return result;}
function validateProfile(input){const experienceLevel=String(input?.experienceLevel||"").toLowerCase(),availableTime=String(input?.availableTime||"");if(!EXPERIENCE_LEVELS.includes(experienceLevel)||!AVAILABLE_TIMES.includes(availableTime))throw error("INVALID_SKILL_PROFILE","Select a valid experience level and available time");const skills=normalizeList(input.skills,normalizeSkill),preferredTaskTypes=normalizeList(input.preferredTaskTypes,normalizeTaskType,10),learningGoals=normalizeList(input.learningGoals||[],value=>{const text=normalizeToken(value);return text&&text.length<=100?text:null;},20),visibility=["private","repository_maintainers","public"].includes(input.visibility)?input.visibility:"private";if(!skills.length)throw error("INVALID_SKILL_PROFILE","Select at least one skill");return{experienceLevel,skills,preferredTaskTypes,availableTime,learningGoals,visibility};}
module.exports={error,normalizeList,validateProfile};
